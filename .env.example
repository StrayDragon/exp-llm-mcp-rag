USE_CN_MIRROR=
PROXY_URL=

DEFAULT_MODEL_NAME=
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

EMBEDDING_KEY=
EMBEDDING_BASE_URL=https://api.siliconflow.cn/v1

# Configuration for a remote MCP server (if used)
# The agent's YAML configuration can refer to these environment variables
# using fields like 'url_env' or 'token_env' (actual YAML fields to be decided by Agent implementation,
# current Agent implementation expects url and token directly or token_env in YAML).
# For example, in your YAML:
# mcp_servers:
#   - id: "my_remote_service"
#     type: "remote"
#     url: ${REMOTE_MCP_URL} # Or agent loads this from env if url is just "REMOTE_MCP_URL"
#     token_env: "REMOTE_MCP_TOKEN" 

REMOTE_MCP_URL=
REMOTE_MCP_TOKEN=
